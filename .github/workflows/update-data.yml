name: Update Stock Data

on:
  schedule:
    # 장중 15분마다 (UTC 14:30~21:00 = KST 23:30~06:00)
    - cron: '30,45 14-20 * * 1-5'
    - cron: '0,15 15-20 * * 1-5'
    - cron: '0 21 * * 1-5'
  workflow_dispatch:

concurrency:
  group: naspick-pipeline
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Consensus Update (Daily Close Only)
        run: |
          HOUR=$(date -u +"%H")
          # Run if >= 21:00 UTC (Daily Close) OR if manually triggered
          # Using >= handles cases where the 21:00 job is delayed to 22:00+
          if [ "$HOUR" -ge "21" ] || [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "📢 Running Daily Consensus & Calendar Update..."
            python update_daily_data.py
          else
            echo "⏭️ Intraday run, skipping consensus update."
          fi
      
      - name: Fetch stock data
        run: python fetch_sp500.py
      
      - name: Update Portfolio
        run: python update_portfolio_value.py
      
      - name: Build Static Pages
        run: python scripts/build_static.py
      
      - name: Update Sitemap
        run: python tools/generate_sitemap.py
      
      - name: Commit and push
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add .
          
          # Check if we ran the full update to tag the commit
          HOUR=$(date -u +"%H")
          if [ "$HOUR" -ge "21" ] || [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
             MSG="📢 [DAILY] Update Consensus + Data: $(date +'%Y-%m-%d %H:%M:%S')"
             # Force commit for daily/manual runs to trigger snapshot
             git commit --allow-empty -m "$MSG"
             git pull --rebase origin main
             git push
          else
             MSG="🤖 Auto-update: $(date +'%Y-%m-%d %H:%M:%S')"
             # Only commit if changes exist for intraday runs
             git diff --quiet && git diff --staged --quiet || (git commit -m "$MSG" && git pull --rebase origin main && git push)
          fi
